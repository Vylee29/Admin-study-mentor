export enum Gender {
  Male = 0,
  Female = 1,
}

export enum UserType {
  STUDENT = 0,
  TUTOR = 1,
}

export enum Status {
  ACTIVE = 0,
  IN_ACTIVE = 1,
}

export enum TutorState {
  IN_ACTIVE = 0,
  CERTIFICATE_SUBMITTED = 1,
  CERTIFICATE_REJECTED = 2,
  CERTIFICATE_APPROVED = 3,
}
